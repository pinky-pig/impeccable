---
import Base from '../../layouts/Base.astro';
import '../../styles/sub-pages.css';
---

<Base
  title="用 Impeccable 做设计"
  description="核心循环：开始、迭代、打磨、维护。带你从空白文件一路走到功能上线，再到偿还设计债。"
  activeNav="designing"
  canonicalPath="/designing"
  bodyClass="sub-page designing-page-body"
>

<div class="designing-page">
  <section class="designing-hero">
    <header class="designing-page-header">
      <span class="designing-page-eyebrow">核心循环</span>
      <h1 class="designing-page-title">用 <em>Impeccable</em> 做设计</h1>
      <p class="designing-page-lede">真正把一个界面交付上线，本来就是一个循环。四个阶段，每一段都有明确的起点。</p>
    </header>

    <div class="designing-loop-wrap" aria-label="四阶段循环">
    <span class="designing-loop-wrap-eyebrow">核心循环</span>
    <div class="designing-loop">

      <a class="designing-loop-node designing-loop-node--start" href="#start">
        <span class="designing-loop-num">01</span>
        <span class="designing-loop-name">开始</span>
        <span class="designing-loop-hint">从空白文件出发，经由 brief，落到真正设计过的功能。</span>
      </a>
      <a class="designing-loop-node designing-loop-node--iterate" href="#iterate">
        <span class="designing-loop-num">02</span>
        <span class="designing-loop-name">迭代</span>
        <span class="designing-loop-hint">在原地继续打磨。可以走命令行，也可以在浏览器里改。</span>
      </a>
      <a class="designing-loop-node designing-loop-node--polish" href="#polish">
        <span class="designing-loop-num">03</span>
        <span class="designing-loop-name">打磨</span>
        <span class="designing-loop-hint">上线前的关卡。audit、clarify、harden 一轮过完。</span>
      </a>
      <a class="designing-loop-node designing-loop-node--maintain" href="#maintain">
        <span class="designing-loop-num">04</span>
        <span class="designing-loop-name">维护</span>
        <span class="designing-loop-hint">在设计债固化之前，先把它还掉。</span>
      </a>
      <div class="designing-loop-wheel" aria-hidden="true">
        <svg class="designing-loop-wheel-svg" viewBox="0 0 100 100" preserveAspectRatio="xMidYMid meet">
          <circle class="designing-loop-wheel-ring" cx="50" cy="50" r="46"/>
          <!-- 12 tick marks at 30° increments. Cardinals (0/90/180/270) stronger. -->
          <line class="designing-loop-wheel-tick designing-loop-wheel-tick--cardinal" x1="50" y1="2.5" x2="50" y2="5.5"/>
          <line class="designing-loop-wheel-tick" x1="73" y1="8.7" x2="72" y2="11.4"/>
          <line class="designing-loop-wheel-tick" x1="91.3" y1="27" x2="88.6" y2="28"/>
          <line class="designing-loop-wheel-tick designing-loop-wheel-tick--cardinal" x1="97.5" y1="50" x2="94.5" y2="50"/>
          <line class="designing-loop-wheel-tick" x1="91.3" y1="73" x2="88.6" y2="72"/>
          <line class="designing-loop-wheel-tick" x1="73" y1="91.3" x2="72" y2="88.6"/>
          <line class="designing-loop-wheel-tick designing-loop-wheel-tick--cardinal" x1="50" y1="97.5" x2="50" y2="94.5"/>
          <line class="designing-loop-wheel-tick" x1="27" y1="91.3" x2="28" y2="88.6"/>
          <line class="designing-loop-wheel-tick" x1="8.7" y1="73" x2="11.4" y2="72"/>
          <line class="designing-loop-wheel-tick designing-loop-wheel-tick--cardinal" x1="2.5" y1="50" x2="5.5" y2="50"/>
          <line class="designing-loop-wheel-tick" x1="8.7" y1="27" x2="11.4" y2="28"/>
          <line class="designing-loop-wheel-tick" x1="27" y1="8.7" x2="28" y2="11.4"/>
          <!-- Clockwise-orbiting accent dot, animates with offset-path. -->
          <circle class="designing-loop-wheel-dot" cx="0" cy="0" r="2.2"/>
        </svg>
        <span class="designing-loop-wheel-arrow designing-loop-wheel-arrow--ne">&#x2198;</span>
        <span class="designing-loop-wheel-arrow designing-loop-wheel-arrow--se">&#x2199;</span>
        <span class="designing-loop-wheel-arrow designing-loop-wheel-arrow--sw">&#x2196;</span>
        <span class="designing-loop-wheel-arrow designing-loop-wheel-arrow--nw">&#x2197;</span>
        <div class="designing-loop-wheel-center">
          <span class="designing-loop-wheel-center-label">designing</span>
          <span class="designing-loop-wheel-center-mark">impeccable</span>
        </div>
      </div>
    </div>
    </div>
  </section>

  <section class="designing-phase" id="start">
    <header class="designing-phase-head">
      <span class="designing-phase-num">01 &middot; 开始</span>
      <h2 class="designing-phase-title">从空白文件，到真正设计过的功能。</h2>
      <p class="designing-phase-sub">三条命令，连成一条弧线。<code>/impeccable teach</code> 负责写 brief，每个项目只做一次。<code>/impeccable shape</code> 先给你一个可以看得见的参考方向。<code>/impeccable craft</code> 再朝着这份参考把代码真正做出来。先是文字，再是图像，最后才是代码。</p>
      <div class="designing-phase-commands">
        <a class="designing-phase-cmd" href="/docs/teach">/impeccable teach</a>
        <a class="designing-phase-cmd" href="/docs/shape">/impeccable shape</a>
        <a class="designing-phase-cmd" href="/docs/craft">/impeccable craft</a>
      </div>
    </header>

    <div class="designing-phase-body designing-start">
      <div class="designing-start-step">
        <span class="designing-start-step-label">teach &middot; 用语言定方向</span>
        <div class="designing-start-grid">
          <div class="docs-viz-file" style="margin:0">
            <div class="docs-viz-file-header">
              <span class="docs-viz-file-name">PRODUCT.md</span>
              <span class="docs-viz-file-status">由 teach 写入</span>
            </div>
            <div class="docs-viz-file-body">
              <div class="docs-viz-file-row">
                <span class="docs-viz-file-k">Register</span>
                <span class="docs-viz-file-v">Product。设计服务于任务本身。</span>
              </div>
              <div class="docs-viz-file-row">
                <span class="docs-viz-file-k">Users</span>
                <span class="docs-viz-file-v">轮值中的 SRE，要快速阅读，而且经常在昏暗环境里工作。</span>
              </div>
              <div class="docs-viz-file-row">
                <span class="docs-viz-file-k">Voice</span>
                <span class="docs-viz-file-v">冷静、临床感、不煽情。</span>
              </div>
              <div class="docs-viz-file-row">
                <span class="docs-viz-file-k">Anti-references</span>
                <span class="docs-viz-file-v">紫色渐变、玻璃拟态、空洞的煽动感。</span>
              </div>
            </div>
          </div>
          <div class="designing-start-grid-prose">
            <p>Teach 会先做一段很短的发现式访谈，聊清楚受众、register、语气和反参考。它会写出 <code>PRODUCT.md</code>；如果项目里已经有代码可扫，还会顺带产出 <code>DESIGN.md</code>。后续每一条命令在生成前都会先读这两份文件。</p>
          </div>
        </div>
      </div>

      <div class="designing-start-step">
        <span class="designing-start-step-label">shape + craft &middot; 用图像定方向</span>
        <p class="designing-start-step-note">现在图像生成已经跨过了“可作参考”的门槛，<code>shape</code> 可以先产出一张你一眼就能判断的品牌工具板，<code>craft</code> 也能朝着高保真参考稿编码，而不是只对着一段文字硬猜。Neo Mirai 就是完整闭环：先生成方向，再实现页面，再进浏览器里继续迭代。</p>

        <div class="designing-visualize-spread">
          <figure class="designing-visualize-plate designing-visualize-plate--brand">
            <a class="designing-visualize-plate-frame" href="/cases/neo-mirai" aria-label="Read the Neo Mirai case study">
              <img src="/assets/openai_image_2_brand.jpg" alt="Auto-generated brand toolkit plate: identity lockups, colour palette, type specimens, icon system, and application mocks for a fictional AI design conference, rendered in warm earth tones." loading="lazy" width="1536" height="1024" />
            </a>
            <figcaption class="designing-visualize-plate-cap">
              <span class="designing-visualize-plate-kind">塑形</span>
              <p class="designing-visualize-plate-note">品牌工具板。识别系统、色盘、字体、图标语言、应用场景、社交卡片、UI 方向，都压进一张图里，让你一眼就能判断。确认过的决定会被写进 <code>DESIGN.md</code>。</p>
            </figcaption>
          </figure>

          <figure class="designing-visualize-plate designing-visualize-plate--hifi">
            <a class="designing-visualize-plate-frame" href="/cases/neo-mirai" aria-label="Read the Neo Mirai case study">
              <img src="/assets/openai_image_2_hifi.jpg" alt="Auto-generated hi-fi landing-page mock: a long vertical editorial comp for a fictional Tokyo AI design conference, in warm earth tones with committed serif display type." loading="lazy" width="864" height="1821" />
            </a>
            <figcaption class="designing-visualize-plate-cap">
              <span class="designing-visualize-plate-kind">可视化</span>
              <p class="designing-visualize-plate-note">高保真参考稿。在第一行 CSS 之前，先把目标长什么样定下来。Craft 对着的是一张具体图像，而不是一份抽象 brief。这就是质变发生的地方。</p>
            </figcaption>
          </figure>

          <figure class="designing-visualize-plate designing-visualize-plate--live">
            <a class="designing-visualize-plate-frame" href="/neo-mirai/" aria-label="Open the Neo Mirai live site">
              <img src="/assets/cases/neo-mirai/live-page.png" alt="Full-page screenshot of the implemented Neo Mirai website." loading="lazy" width="1440" height="3013" />
            </a>
            <figcaption class="designing-visualize-plate-cap">
              <span class="designing-visualize-plate-kind">上线</span>
              <p class="designing-visualize-plate-note">线上版本。参考稿最终变成了语义化标记、重做后的素材、响应式修正、导航状态、嘉宾轮播行为，以及经浏览器验证的完整打磨。<a href="/neo-mirai/">打开线上页面</a>。</p>
            </figcaption>
          </figure>
        </div>

        <p class="designing-visualize-foot">前两张图由 <strong>OpenAI GPT Image 2</strong> 生成，第三张则是已经实现完成的 Neo Mirai 页面。通过 Codex、Gemini CLI 和兼容的 harness，<strong>Gemini Nano Banana Pro</strong>、<strong>Imagen 4 Ultra</strong> 与 <strong>Grok Imagen</strong> 也能以同样方式接入这条工作流。</p>
      </div>
    </div>
  </section>

  <section class="designing-phase" id="iterate">
    <header class="designing-phase-head">
      <span class="designing-phase-num">02 &middot; 迭代</span>
      <h2 class="designing-phase-title">把已经存在的东西继续磨好。</h2>
      <p class="designing-phase-sub">只要某个界面已经存在，你做的就是迭代。这里有两条路：一种是用明确命名的命令改具体维度，另一种是用 Live Mode 做可视化探索。</p>
    </header>

    <div class="designing-phase-body">
      <div class="designing-iterate-split">
        <div class="designing-iterate-col">
          <span class="designing-iterate-kind">命令行</span>
          <h3 class="designing-iterate-name">当这次修改有明确名字时。</h3>
          <p class="designing-iterate-when">直接打一条命令，让 skill 替你调用某个具体学科。最适合你已经知道问题属于什么：排版、布局、色彩或动效。</p>
          <div class="designing-iterate-terminal" aria-hidden="true">
            <div class="designing-iterate-terminal-line"><span class="designing-iterate-terminal-prompt">$</span><span>/impeccable polish pricing</span></div>
            <div class="designing-iterate-terminal-line"><span class="designing-iterate-terminal-prompt">$</span><span>/impeccable bolder hero</span></div>
            <div class="designing-iterate-terminal-line"><span class="designing-iterate-terminal-prompt">$</span><span>/impeccable typeset checkout</span></div>
          </div>
        </div>

        <div class="designing-iterate-col">
          <span class="designing-iterate-kind">实时模式</span>
          <h3 class="designing-iterate-name">当这个修改更适合“指给它看”时。</h3>
          <p class="designing-iterate-when">在浏览器里选中元素，画一笔、写一句，再点开始。系统会给你三个生产级变体。接受其中一个，它就会写回源码。</p>
          <div class="designing-iterate-live" aria-hidden="true">
            <div class="docs-viz-live-frame" style="max-width:100%">
              <div class="docs-viz-live-chrome">
                <span class="docs-viz-live-dot"></span>
                <span class="docs-viz-live-dot"></span>
                <span class="docs-viz-live-dot"></span>
                <span class="docs-viz-live-url">localhost:3000</span>
              </div>
              <div class="docs-viz-live-stage">
                <div class="docs-viz-live-target">
                  <span class="docs-viz-live-kicker">第 04 期</span>
                  <h4 class="docs-viz-live-title">偶尔来一封，<em>就好</em>。</h4>
                  <button class="docs-viz-live-btn" type="button">寄给我一封</button>
                </div>
                <div class="docs-viz-live-outline"></div>
                <div class="docs-viz-live-ctx">
                  <button class="docs-viz-live-ctx-nav" type="button">&#x2039;</button>
                  <span class="docs-viz-live-ctx-counter">2 / 3</span>
                  <button class="docs-viz-live-ctx-nav" type="button">&#x203A;</button>
                  <span class="docs-viz-live-ctx-divider"></span>
                  <button class="docs-viz-live-ctx-accept" type="button">接受</button>
                </div>
                <div class="docs-viz-live-gbar">
                  <span class="docs-viz-live-gbar-brand">/</span>
                  <span class="docs-viz-live-gbar-btn is-active">选取</span>
                  <span class="docs-viz-live-gbar-divider"></span>
                  <span class="docs-viz-live-gbar-x">&#x2715;</span>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>

      <table class="designing-iterate-table">
        <caption>什么时候该用哪一个</caption>
        <tbody>
          <tr>
            <th scope="row">你知道哪里不对，但说不清它叫什么</th>
            <td><a href="/live-mode">/impeccable live</a></td>
          </tr>
          <tr>
            <th scope="row">针对某个明确维度下手：排版、布局、色彩、动效</th>
            <td><a href="/docs/typeset">/typeset</a> &middot; <a href="/docs/layout">/layout</a> &middot; <a href="/docs/colorize">/colorize</a> &middot; <a href="/docs/animate">/animate</a></td>
          </tr>
          <tr>
            <th scope="row">想并排比较三个方向</th>
            <td><a href="/live-mode">/impeccable live</a></td>
          </tr>
          <tr>
            <th scope="row">想问一句“这东西到底行不行”</th>
            <td><a href="/docs/critique">/impeccable critique</a></td>
          </tr>
          <tr>
            <th scope="row">把太保守的设计推起来，或把太吵的设计压回去</th>
            <td><a href="/docs/bolder">/bolder</a> &middot; <a href="/docs/quieter">/quieter</a></td>
          </tr>
        </tbody>
      </table>
    </div>
  </section>

  <section class="designing-phase" id="polish">
    <header class="designing-phase-head">
      <span class="designing-phase-num">03 &middot; 打磨</span>
      <h2 class="designing-phase-title">上线前必须过的一道关。</h2>
      <p class="designing-phase-sub">任何东西上线前，都先顺着跑完这三条命令。它们不是帮你重做设计，而是找出还有哪些地方没改到位。</p>
    </header>

    <div class="designing-phase-body">
      <div class="designing-polish">
        <div class="designing-polish-band">
          <span class="designing-polish-band-label">上线前</span>
          <div class="designing-polish-band-cmds">
            <a href="/docs/audit">audit</a>
            <a href="/docs/clarify">clarify</a>
            <a href="/docs/harden">harden</a>
          </div>
          <span class="designing-polish-band-meta">03 &middot; 04</span>
        </div>
        <div class="designing-polish-grid">
          <div class="designing-polish-col">
            <h3 class="designing-polish-name">先打分。</h3>
            <p class="designing-polish-desc">五个维度统一按 0 到 4 评分：无障碍、性能、主题、一致性、响应式、anti-patterns。每个发现再按 P0 到 P3 标记优先级。</p>
          </div>
          <div class="designing-polish-col">
            <h3 class="designing-polish-name">再改文案。</h3>
            <p class="designing-polish-desc">标签、报错、空状态说明、微文案，都会按 PRODUCT.md 里定义的受众重新校准。</p>
          </div>
          <div class="designing-polish-col">
            <h3 class="designing-polish-name">最后拿真实世界去撞它。</h3>
            <p class="designing-polish-desc">60 个字符的人名、德语产品标题、数十亿级价格、500 错误、离线场景。线上数据从来不会像设计稿那样乖。</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="designing-phase" id="maintain">
    <header class="designing-phase-head">
      <span class="designing-phase-num">04 &middot; 维护</span>
      <h2 class="designing-phase-title">设计债是真的，要主动还。</h2>
      <p class="designing-phase-sub">功能上线以后，漂移一定会发生。这里有两条命令，专门用来在它固化之前把缺口补上。</p>
    </header>

    <div class="designing-phase-body">
      <div class="designing-maintain">
        <figure class="designing-maintain-tile">
          <div class="designing-maintain-stage">
            <div class="designing-extract-viz" aria-hidden="true">
              <div class="designing-extract-before">
                <span class="designing-extract-btn">Subscribe</span>
                <span class="designing-extract-btn">Submit</span>
                <span class="designing-extract-btn">Join</span>
                <span class="designing-extract-btn">Send</span>
                <span class="designing-extract-btn">Go</span>
                <span class="designing-extract-btn">OK</span>
              </div>
              <span class="designing-extract-arrow">&rarr;</span>
              <span class="designing-extract-after">Button</span>
            </div>
          </div>
          <figcaption class="designing-maintain-caption">
            <span class="designing-maintain-label"><a href="/docs/extract">/impeccable extract</a></span>
            <h3 class="designing-maintain-name">把漂移收束起来。</h3>
            <p class="designing-maintain-desc">找出那些在同一意图下被重复三次以上的模式，并把它们提炼成 tokens 与 primitives。</p>
          </figcaption>
        </figure>

        <figure class="designing-maintain-tile">
          <div class="designing-maintain-stage">
            <div class="designing-designmd-preview" aria-hidden="true">
              <div class="designing-designmd-preview-line"><span class="designing-designmd-preview-num">01</span><span>概览 (Overview)</span></div>
              <div class="designing-designmd-preview-line"><span class="designing-designmd-preview-num">02</span><span>色彩 (Colors)</span></div>
              <div class="designing-designmd-preview-line"><span class="designing-designmd-preview-num">03</span><span>排版 (Typography)</span></div>
              <div class="designing-designmd-preview-line"><span class="designing-designmd-preview-num">04</span><span>层次 (Elevation)</span></div>
              <div class="designing-designmd-preview-line"><span class="designing-designmd-preview-num">05</span><span>组件 (Components)</span></div>
              <div class="designing-designmd-preview-line"><span class="designing-designmd-preview-num">06</span><span>规范与禁忌 (Do's &amp; Don'ts)</span></div>
            </div>
          </div>
          <figcaption class="designing-maintain-caption">
            <span class="designing-maintain-label"><a href="/docs/document">/impeccable document</a></span>
            <h3 class="designing-maintain-name">把系统重新抓回来。</h3>
            <p class="designing-maintain-desc">扫描 tokens、组件和渲染结果，重新写出一份符合规范的 DESIGN.md。</p>
          </figcaption>
        </figure>
      </div>
    </div>
  </section>

  <section class="designing-phase designing-phase--appendix" aria-label="品牌还是产品">
    <header class="designing-phase-head">
      <span class="designing-phase-num">两条车道</span>
      <h2 class="designing-phase-title">品牌，还是产品。</h2>
      <p class="designing-phase-sub">它们有各自不同的默认词汇和判断方式。Impeccable 会在每次命令开始前，根据你的任务提示和 <code>PRODUCT.md</code> 自动判断该走哪一条，所以 <code>typeset</code>、<code>animate</code>、<code>colorize</code> 等命令都会跟着调整输出。大多数时候你不需要手动指定。</p>
    </header>

    <div class="designing-phase-body">
      <div class="designing-lanes">
        <section class="designing-lane">
          <span class="designing-lane-kind">品牌</span>
          <p class="designing-lane-rule">设计本身就是产品。营销页、落地页、编辑型页面、长内容、作品集都属于这一侧。</p>
          <div class="designing-lane-mock designing-lane-mock--brand" aria-hidden="true">
            <span class="designing-lane-mock-label">第 04 期 &middot; 专递</span>
            <span class="designing-lane-mock-title">偶尔来一封。</span>
          </div>
        </section>
        <section class="designing-lane">
          <span class="designing-lane-kind">产品</span>
          <p class="designing-lane-rule">设计服务于任务。App UI、后台、仪表盘、工具型产品都属于这里。</p>
          <div class="designing-lane-mock designing-lane-mock--product" aria-hidden="true">
            <span class="designing-lane-mock-label">通讯</span>
            <span class="designing-lane-mock-title">订阅最新更新</span>
          </div>
        </section>
      </div>
      <a class="designing-lane-link" href="/tutorials/brand-vs-product">阅读 brand-vs-product 教程 &rarr;</a>
    </div>
  </section>

  <section class="designing-phase designing-phase--appendix" aria-label="需要避免的事">
    <header class="designing-phase-head">
      <span class="designing-phase-num">常见错误</span>
      <h2 class="designing-phase-title">这些事尽量别做。</h2>
      <p class="designing-phase-sub">一份针对使用 anti-patterns 工具时最常见误区的清单。</p>
    </header>

    <div class="designing-phase-body">
      <ul class="designing-avoid">
        <li>
          <span class="designing-avoid-x" aria-hidden="true">&times;</span>
          <div>
            <span class="designing-avoid-title">同时运行 Impeccable 和 Anthropic 的 frontend-design skill</span>
            <p class="designing-avoid-desc">Anthropic 在 Claude Code 里还在推荐他们自己的 skill，但它已经长期缺乏维护，和当前推荐模式有明显滞后。两者一起跑会在词汇和判断上互相打架，彼此抵消。二选一即可。</p>
          </div>
        </li>
        <li>
          <span class="designing-avoid-x" aria-hidden="true">&times;</span>
          <div>
            <span class="designing-avoid-title">把每一条命令都 pin 回快捷方式</span>
            <p class="designing-avoid-desc">Pin 的作用是把 <code>/audit</code>、<code>/polish</code>、<code>/critique</code> 这类命令重新变成快捷入口。但如果全都 pin 回来，你又会把 v3.0 好不容易收拢干净的 <code>/</code> 菜单重新炸开。只 pin 你每天真的会用到的两三条就够了。</p>
          </div>
        </li>
        <li>
          <span class="designing-avoid-x" aria-hidden="true">&times;</span>
          <div>
            <span class="designing-avoid-title">跳过 <code>teach</code></span>
            <p class="designing-avoid-desc">没有 PRODUCT.md 和 DESIGN.md，命令也能跑，但默认就会更容易滑回通用 SaaS 模板。有上下文时，输出下限会明显更高。只要先跑一次 teach，后面每一条命令都会受益。</p>
          </div>
        </li>
        <li>
          <span class="designing-avoid-x" aria-hidden="true">&times;</span>
          <div>
            <span class="designing-avoid-title">把它当成 linter 用</span>
            <p class="designing-avoid-desc">Impeccable 是带观点的设计搭档，不是只会打勾或报错的验证器。它会坚持自己的判断，但你完全可以基于真实理由反驳它，它也会跟着调整。真正会让结果变差的，是没有理由地忽略它的判断。</p>
          </div>
        </li>
      </ul>
    </div>
  </section>

  <nav class="designing-cta" aria-label="接下来去哪里">
    <a class="designing-cta-card" href="/docs/teach">
      <span class="designing-cta-card-kind">新项目</span>
      <h2 class="designing-cta-card-title">先从 <em>teach</em> 开始</h2>
      <p class="designing-cta-card-desc">五分钟的发现式访谈，换来一份 PRODUCT.md 和一份 DESIGN.md。从这里开始，后续每条命令都会更准。</p>
    </a>
    <a class="designing-cta-card" href="/tutorials">
      <span class="designing-cta-card-kind">按场景学习</span>
      <h2 class="designing-cta-card-title">打开一篇 <em>教程</em></h2>
      <p class="designing-cta-card-desc">四篇短教程：快速开始、Live Mode、带 overlay 的 critique，以及 brand vs product。</p>
    </a>
  </nav>
</div>

<script is:inline>
  // Copy buttons on rendered code blocks
  document.addEventListener('click', (e) => {
    const btn = e.target.closest('[data-copy]');
    if (!btn) return;
    const text = btn.getAttribute('data-copy');
    if (!text) return;
    navigator.clipboard.writeText(text).then(() => {
      btn.classList.add('is-copied');
      setTimeout(() => btn.classList.remove('is-copied'), 1500);
    }).catch(() => {});
  });

  // Mobile sidebar toggle (shown on narrow viewports, hidden on desktop).
  document.addEventListener('click', (e) => {
    const toggle = e.target.closest('.skills-sidebar-toggle');
    if (!toggle) return;
    const expanded = toggle.getAttribute('aria-expanded') === 'true';
    toggle.setAttribute('aria-expanded', String(!expanded));
  });

  // Before/after split-compare: drag on touch, hover OR drag on mouse.
  // Pointer events attach to the padded .split-comparison wrapper so
  // there is a ~20px invisible buffer around the visible box. The
  // divider only snaps back when the pointer leaves that outer buffer.
  (function initSplitCompare() {
    const wrappers = document.querySelectorAll('.split-comparison');
    if (wrappers.length === 0) return;
    const hasHover = matchMedia('(hover: hover)').matches;
    const DEFAULT_POSITION = 50;

    for (const wrapper of wrappers) {
      const container = wrapper.querySelector('.split-container');
      const splitAfter = wrapper.querySelector('.split-after');
      const splitDivider = wrapper.querySelector('.split-divider');
      if (!container || !splitAfter || !splitDivider) continue;

      const tanAngle = Math.tan(10 * Math.PI / 180);
      let skewOffset = 8;
      const recalcSkew = () => {
        const r = container.getBoundingClientRect();
        if (r.width > 0 && r.height > 0) {
          skewOffset = 50 * r.height * tanAngle / r.width;
        }
      };
      recalcSkew();
      window.addEventListener('resize', recalcSkew, { passive: true });

      let targetX = DEFAULT_POSITION;
      let currentX = DEFAULT_POSITION;
      let rafId = null;

      const paint = (pct) => {
        const x = Math.max(-skewOffset, Math.min(100 + skewOffset, pct));
        splitAfter.style.clipPath =
          `polygon(${x + skewOffset}% 0%, 100% 0%, 100% 100%, ${x - skewOffset}% 100%)`;
        splitDivider.style.left = `${x}%`;
      };

      const step = () => {
        currentX += (targetX - currentX) * 0.2;
        if (Math.abs(targetX - currentX) < 0.1) {
          currentX = targetX;
          rafId = null;
        } else {
          rafId = requestAnimationFrame(step);
        }
        paint(currentX);
      };

      const setTarget = (pct) => {
        targetX = pct;
        if (rafId === null) rafId = requestAnimationFrame(step);
      };

      paint(DEFAULT_POSITION);

      // Percentage is always relative to the VISIBLE .split-container,
      // not the padded .split-comparison wrapper. The pointer event
      // target is the wrapper but the clip-path math uses the inner box.
      const pctFromClientX = (clientX) => {
        const rect = container.getBoundingClientRect();
        return ((clientX - rect.left) / rect.width) * 100;
      };

      let hovering = false;
      let dragging = false;

      wrapper.addEventListener('pointerenter', (e) => {
        if (hasHover && e.pointerType === 'mouse') {
          hovering = true;
        }
      });

      wrapper.addEventListener('pointerdown', (e) => {
        dragging = true;
        wrapper.setPointerCapture(e.pointerId);
        setTarget(pctFromClientX(e.clientX));
      });

      wrapper.addEventListener('pointermove', (e) => {
        if (dragging || hovering) {
          setTarget(pctFromClientX(e.clientX));
        }
      });

      const endDrag = (e) => {
        if (dragging) {
          dragging = false;
          try { wrapper.releasePointerCapture(e.pointerId); } catch {}
        }
      };

      wrapper.addEventListener('pointerup', endDrag);
      wrapper.addEventListener('pointercancel', endDrag);

      wrapper.addEventListener('pointerleave', (e) => {
        endDrag(e);
        if (hovering) {
          hovering = false;
          setTarget(DEFAULT_POSITION);
        }
      });
    }
  })();
</script>

</Base>
