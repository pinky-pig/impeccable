---
import Base from '../layouts/Base.astro';
import '../styles/sub-pages.css';
---

<Base title="隐私政策 - Impeccable" noIndex={true} bodyClass="sub-page">

<style>
  .privacy-content { max-width: 680px; margin: 0 auto; padding: 2rem 1.5rem 4rem; }
  .privacy-content h1 { font-family: var(--font-display); font-size: 2.25rem; margin-bottom: 0.5rem; letter-spacing: -0.01em; }
  .privacy-content h2 { font-size: 1.125rem; margin-top: 2rem; font-weight: 600; }
  .privacy-content p, .privacy-content ul { color: var(--color-charcoal); margin-top: 0.5rem; }
  .privacy-content ul { padding-left: 1.25rem; }
  .privacy-content li { margin-top: 0.25rem; }
  .privacy-content a { color: var(--color-accent); text-decoration: underline; text-underline-offset: 3px; }
  .privacy-content a:hover { color: var(--color-accent-hover); }
  .privacy-content .updated { color: var(--color-ash); font-size: 0.875rem; margin-bottom: 2rem; font-style: italic; }
</style>

<div class="privacy-content">
  <h1>隐私政策</h1>
  <p class="updated">最后更新：2026 年 5 月 11 日</p>

  <h2>Impeccable 是什么</h2>
  <p>Impeccable 是一组开源的 agent skills（文本文件），运行在你的本地 AI 编程工具中。skill 本身不会采集数据，也不会主动发起网络请求。网站层面的统计方式会在下文单独说明。</p>

  <h2>网站统计</h2>
  <p>Impeccable 网站（<a href="https://impeccable.style">impeccable.style</a>）使用来自 <code>cloud.umami.is</code> 的 Umami 以及 Google Analytics，用于了解基础访问情况，例如页面浏览量与来源站点。我们不会把这些统计能力用于广告投放或再营销。</p>

  <h2>下载</h2>
  <p>当你从网站下载 skill 打包文件时，我们会记录一次下载事件（例如下载的是哪个包、发生时间），用于统计使用情况。这些记录不附带个人身份信息。</p>

  <h2>Claude Code 插件</h2>
  <p>当 Impeccable 以 Claude Code 插件形式安装时，它会完全在你的本地 Claude Code 会话中运行，不会向 Impeccable 的服务器发送数据。Claude Code 应用本身受 Anthropic 的隐私政策约束。</p>

  <h2>Chrome 扩展</h2>
  <p>Impeccable 的 Chrome DevTools 扩展完全运行在你的浏览器里。所有 anti-pattern 检测都只在你当前查看的页面本地完成。页面内容、URL 和检测结果都不会发送到任何外部服务器。</p>
  <p>扩展会使用 Chrome 内建的同步存储（<code>chrome.storage.sync</code>）保存你的规则偏好设置，也就是哪些检测被启用、哪些被禁用。该设置会通过你的 Google 账号在不同 Chrome 实例之间同步。除此之外，不会额外存储或传输其他数据。</p>
  <p>扩展会申请以下权限：</p>
  <ul>
    <li><strong>activeTab / scripting</strong> - 用于把检测脚本注入到你当前检查的页面</li>
    <li><strong>storage</strong> - 用于保存你的规则偏好设置</li>
    <li><strong>webNavigation</strong> - 用于在你导航到新页面时自动重新扫描</li>
    <li><strong>Host permissions（所有 URL）</strong> - 让检测器可以运行在你选择检查的任意网站上</li>
  </ul>

  <h2>GitHub</h2>
  <p>源码托管在 GitHub 上。你与仓库的交互（例如 issue、pull request、star）受 <a href="https://docs.github.com/en/site-policy/privacy-policies/github-general-privacy-statement">GitHub 隐私政策</a> 约束。</p>

  <h2>联系</h2>
  <p>如果你对这份政策有疑问，可以在 <a href="https://github.com/pbakaus/impeccable">GitHub</a> 提交 issue，或联系 <a href="https://x.com/pbakaus">@pbakaus</a>。</p>
</div>

</Base>
